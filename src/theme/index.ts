
const colors = {
  headerBackground: '#4f0de4',
  menuHeaderBackground: '#a8a4a4',
  red: 'red',
  white: 'white',
};

const typography = {
  stdFontSize: 14,
  largeFontSize: 22,
};

const spacing = {
  xSmallMargin: 4,
  smallMargin: 10,
  mediumMargin: 12,
  largeMargin: 20,
};

const theme = {
  colors,
  typography,
  spacing,
};

export default theme;
