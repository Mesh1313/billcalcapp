import theme from "@app/theme";
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: theme.spacing.mediumMargin,
    paddingHorizontal: theme.spacing.largeMargin,
  },
});

export default styles;
